#include <iostream>
#include "mathUtils.h"



int main()
{
	int val = sum(5, 2);
	float fVal = sum(5.5f, 1.2f); 

	int vval = summ(-5, 6, 2);

	 inPut(4, 1);
	 int min = ();
	return 0;
}

